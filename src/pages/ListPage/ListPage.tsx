const ListPage = () => {
  return (
    <div>
      {/* Page Header */}
      {/* Filters up the top */}
      {/* Sorting component middle left */}
      {/* Container that loads EmployeeCards of a short description, with pagination */}
      {/* Pagination Buttons */}
    </div>
  );
};

export default ListPage;
